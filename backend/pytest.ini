[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --strict-markers --tb=short
markers =
    determinism: tests for deterministic execution
    audit: tests for audit trail integrity
    timelock: tests for time-lock enforcement
    isolation: tests for tenant isolation
